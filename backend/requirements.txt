Flask==3.1.2
flask-cors==6.0.2
Flask-SQLAlchemy==3.1.1
python-dotenv==1.0.1
Werkzeug==3.1.4
PyJWT==2.8.0
gunicorn

# AI Providers (choose one or install all)
# Groq (optional; required when AI_PROVIDER=groq or a request explicitly selects Groq)
groq
# Gemini (free tier): https://ai.google.dev
google-generativeai

# Ollama (local, completely free): https://ollama.ai
# No pip package needed, but install Ollama app separately

# Hugging Face (free tier): https://huggingface.co/settings/tokens
huggingface-hub

# Legacy LangChain packages (optional, not needed for current implementation)
langchain
langchain-google-genai
langchain-core
